pipeline {
  
  agent any 

  environment {
    PROJECT_NAME = "jenkins-multibranch-pipeline"
  }

  stages {
    stage('checking') {
      steps {
        sh """
        echo " everything working "
        """
      }
    }
  }
}