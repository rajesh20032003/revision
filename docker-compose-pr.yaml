#for 4 services frontend , backend , db ,

version: "3.9"

services: 
  frontend-service:
    build: ./services/frontend-service 
    port:
      - "8080:8080" 
    environment:
      - PORT=8080
      - BACKEND_SERVICE_URL=http://backend-service:8080
  backend-service:
  db-service: